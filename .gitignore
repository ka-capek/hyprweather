# Vygenerované náhledy — kdykoli znovu vyrobitelné přes ./shot.sh nebo ./run.sh
preview-*.png

# Uživatelská data obalu (cache počasí, zvolené město) žijí v ~/.config
# Dočasné profily prohlížeče z screenshotů
*.log

# Nástroje a výstupy balení — Electron, electron-builder a hotové balíčky
/node_modules/
/release/
